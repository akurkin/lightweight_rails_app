machine:
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.3.0

test:
  override:
    - docker-compose -f docker-compose.development.yml run -e RAILS_ENV=test web rake db:create rake db:migrate
    - docker-compose -f docker-compose.development.yml run -e RAILS_ENV=test web rspec
database:
  override:
   - echo 'skipping db setup'
