machine:
  services:
    - docker
  pre:
    - sudo curl -sSL https://get.docker.com/ | sh

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

test:
  override:
    - docker-compose -f docker-compose.development.yml run web rspec
database:
  override:
   - echo 'skipping db setup'
